// src/components/common/index.ts
export { default as LoadingSpinner } from './LoadingSpinner';
export { default as AlertMessage } from './AlertMessage';
export type { AlertType } from './AlertMessage'; // Export type if needed elsewhere

// Add exports for other common components as you create them (e.g., Button, Modal, Input)